name: all_perks_streak_helper
description: All killer perks helper

publish_to: 'none'
version: 1.3.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  path_provider: ^2.0.11 
  file_picker: ^5.2.5
  flutter_colorpicker: ^1.0.3
  get: ^4.6.5
  shared_preferences: ^2.0.16
  http: ^0.13.5
  html: ^0.15.1
  reorderables: ^0.6.0
  context_menus: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/killers/
    - assets/images/perks/